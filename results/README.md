# results

Small, version-controlled experiment artifacts: manifests, human-study trials,
and objective result JSONs. **No audio and no large intermediates** are stored
here (they are reproducible from scripts and are gitignored).

Each subdirectory corresponds to one pilot/dataset run and contains its own
`README.md` documenting provenance and interpretation.
